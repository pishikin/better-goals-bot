# Telegram Bot Token (get from @BotFather)
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Database URL (SQLite)
# LOCAL: Use relative path for development
DATABASE_URL="file:./data/app.db"
# PRODUCTION: MUST use absolute path! Prisma resolves relative paths from
# schema.prisma location, not from cwd. Example for server:
# DATABASE_URL="file:/var/app/goals-bot/data/app.db"

# Environment
NODE_ENV=development

# Default timezone
DEFAULT_TIMEZONE=UTC
